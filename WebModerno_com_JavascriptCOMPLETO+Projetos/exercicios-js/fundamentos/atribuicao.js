const a = 7
let b = 3

// Símbolo da operação antes e '=' depois :)

b += a // Acrescentando o valor de 'a' a variável b; b = b + a
console.log(b)

b -= 4 // Subtraindo 4 de  b; b = b - 4
console.log(b)

b *= 2 // Multiplicando b por 2; b = b * 2
console.log(b)

b /= 2 // Dividindo b por 2; b = b / 2
console.log(b)

b %= 2 // Resto da divisão de b por 2; b = b % 2
console.log(b)